{
  "fields": [
    {
      "name": "worldName",
      "label": "World Name",
      "type": "text",
      "required": true,
      "defaultValue": "SmarthomeWorld",
      "description": "Name of the Terraria world to create or load"
    },
    {
      "name": "maxPlayers",
      "label": "Max Players",
      "type": "select",
      "required": true,
      "defaultValue": "8",
      "options": ["4", "8", "16"],
      "description": "Maximum number of players allowed on the server"
    },
    {
      "name": "difficulty",
      "label": "Difficulty",
      "type": "select",
      "required": true,
      "defaultValue": "1",
      "options": ["0", "1", "2", "3"],
      "description": "0=Classic, 1=Expert, 2=Master, 3=Journey"
    },
    {
      "name": "worldSize",
      "label": "World Size",
      "type": "select",
      "required": true,
      "defaultValue": "2",
      "options": ["1", "2", "3"],
      "description": "1=Small, 2=Medium, 3=Large (only applies to new worlds)"
    },
    {
      "name": "password",
      "label": "Server Password",
      "type": "text",
      "required": false,
      "defaultValue": "",
      "description": "Optional password to protect your server"
    }
  ]
}
